node_modules/
build/
target/
*.wasm

# Note: src/parser.c, src/grammar.json, src/node-types.json, and
# src/tree_sitter/ are generated by `tree-sitter generate` but ARE
# committed — Zed and other consumers expect the generated parser to
# be present without needing the tree-sitter CLI to regenerate it.
